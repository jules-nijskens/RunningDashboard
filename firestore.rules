rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Restrict read/write access to the specific authorized owner only
    match /{document=**} {
      allow read, write: if request.auth != null && request.auth.token.email == 'jls.nijskens@gmail.com';
    }
  }
}
