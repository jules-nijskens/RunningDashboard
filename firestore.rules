rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow the owner to read and write everything based on email
    match /{document=**} {
      allow read, write: if request.auth != null && request.auth.token.email == 'jls.nijskens@gmail.com';
    }
  }
}
